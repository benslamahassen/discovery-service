name: Unit Tests

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: checkout repo
        uses: actions/checkout@v3

      - name: install depes
        run: npm i

      - name: build project
        run: npm run compile

      - name: run unit tests
        run: npm run test
